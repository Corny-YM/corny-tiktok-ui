const images = {
    // file svg is diffirent from file png
    // => png don't have to .default
    logo: require('~/assets/images/logo.svg').default,
    noImage: require('~/assets/images/no-image.png'),
};

export default images;
